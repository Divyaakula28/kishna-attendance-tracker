import React from 'react';

const PeacockFeatherIcon = ({ className = '', style = {} }) => {
  return (
    <i 
      className={`fa-solid fa-peacock-feather ${className}`} 
      style={style}
    ></i>
  );
};

export default PeacockFeatherIcon;
