import React from 'react';

const NamamIcon = ({ className = '', style = {} }) => {
  return (
    <i 
      className={`fa-solid fa-namam ${className}`} 
      style={style}
    ></i>
  );
};

export default NamamIcon;
